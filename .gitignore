/target
*.db
*.db-shm
*.db-wal
octopus.db*

# 派生向量库（#27）：可从命令日志重建，不入版本控制
octopus-vectors.duckdb*

# fastembed 模型缓存（M1）：91MB 二进制权重，首次运行时自动下载
.fastembed_cache/

# 资产库（#28）：图片内容寻址存储，不入版本控制
/assets/

# 本地配置文件与密钥（0600）
config.json
*.log

# SillyTavern 测试夹具（本地验证素材，不入版本控制）
/silly/

# 临时脚本（本地调试用，不入版本控制）
/.scratch/
