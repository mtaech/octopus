/target
*.db
*.db-shm
*.db-wal
octopus.db*

# 资产库（#28）：图片内容寻址存储，不入版本控制
/assets/

# 本地配置文件与密钥（0600）
config.json
*.log

# SillyTavern 测试夹具（本地验证素材，不入版本控制）
/silly/

# 临时脚本（本地调试用，不入版本控制）
/.scratch/

# AgentTeams 会话状态目录（运行时产物，不入版本控制）
/.agent-teams/
