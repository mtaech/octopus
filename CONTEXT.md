# Octopus：通用 AI RPG 软件

一个通用 AI RPG 软件：玩家用故事书自己定义故事（世界 / 角色 / 规则），接入 AI 后由 AI 演绎剧情与角色，玩家沉浸体验自己创作的故事。不预设题材与规则体系；内置代码引擎仅作规则与结算的执行层。

## Language

> 术语下方的 `_实现_` 标注该概念在当前代码中的落地状态；未标注表示与实现无冲突的纯领域定义。权威模型（命令日志 / 叙事条目 / 快照 / 分支 / 升格 / 遗留区）的落地细节见 [会话持久化与聊天完善规范](./docs/session-persistence.md)。

**故事书 (Storybook)**:
玩家创作的设定集——世界设定、人物、技能/法术、物品、势力、关系；是引擎与 AI 的单一事实来源。
_Avoid_: 剧本、模组（暗示线性剧本；故事书强调开放设定）

**故事开头 (Opening)**:
故事书中可选声明的开篇旁白——玩家开档后读到的第一段叙事，先于世界前提呈现；作为新档演出流的开场（缺省回落为「世界前提」，见「演绎」）。与「世界前提」的分野：前提讲世界是什么样，开头讲故事从哪里开始。
_实现_: 落在 `world.opening`（编辑器 A/B 范式可编辑；游玩页初始化把第一个 narrate 给 opening）；校验对「有场景骨架但未写开头」给 warning。

**版次 (Revision)**:
故事书的内容版本——编辑器显式「发布」时 +1，玩家可见；区别于 schema_version（JSON 格式版本，引擎 / 编辑器发版驱动）。存档记录其基于的故事书与版次。
_Avoid_: 版本（过宽，易与 schema 版本混淆）

**草稿 (Draft)**:
编辑中的故事书工作副本，相对「版次」的未发布态；自动保存不 bump 版次，显式「发布」才 bump 版次。
_Avoid_: 未保存内容（过窄，草稿是持久的工作副本而非内存暂存）

**发布 (Publish)**:
编辑器操作——把当前草稿定型为新的已发布版次（版次 +1），是唯一 bump 版次的入口；存档只能基于已发布的版次（未发布的故事书在游玩侧不可用）；发布后草稿成为下一版次的基座。
_Avoid_: 保存、导出（发布有版本语义；草稿自动保存不 bump 版次）

**AI 主持 (Game AI)**:
单一 AI：负责故事走向、旁白与世界响应，并按人物设定扮演所有非玩家角色（NPC）的言行；在故事书的结构化骨架内即兴导演。
_Avoid_: 主线 AI、角色 AI（多 AI 分工已移除）、GM AI、导演 AI、NPC AI

**人物设定 (Persona)**:
人物模板中随回合注入提示词的「人格档案」——背景 / 性格 / 外观 / 对话示例；只含发给 AI 的字段。模板上的**作者注释 (Notes)** 是给创作者的备注，永不注入（编辑器以「不发给 AI」标记）。
_实现_: `PersonaView`（octopus-engine `ports.rs`）；仅注入在场角色，上限 6 个；`example_dialogues` 是其 few-shot 风格样板（对标 SillyTavern 角色卡的 Example Messages）。
_Avoid_: 角色卡（SillyTavern 术语，且其字段混含不发给 AI 的注释）

**采样预设 (Preset)**:
一组具名的采样参数档（温度 / Top-P / 惩罚 / 停止序列），如「创意写作 / 日常 RP / 严肃对话 / NPC 模式」；只影响新生成，不参与命令日志重放。
_实现_: `RoleConfig` 的 `top_p` / `presence_penalty` / `frequency_penalty` / `repetition_penalty` / `stop` / `preset`；前端 `lib/aiPresets.ts` 提供四套档位。
_Avoid_: 模型（预设只调参数，不换模型）

**思考强度 (Reasoning Effort)**:
随模型下发的思考预算档（minimal / low / medium / high），分「角色默认」与「本存档覆盖」两级；缺省表示不发该参数、由供应商决定。
_实现_: 全局 `RoleConfig.reasoning_effort`；本存档 `SaveSettings.reasoning_effort`（saves 表列，随模型一起持久化）；经 rig `additional_params` 下发。**可选档位来自模型目录快照 `thinkingLevelMap`（pi.dev），并可由用户在供应商的模型条目上逐字段覆盖**（`ModelEntry.ctx/max_out/reasoning/tl`，供小中转站自定义）；`off` / wire 为 null 表示不发送该参数（用供应商默认）。
_Avoid_: 思考开关（它是强度档，不是开关）

**思考链 (Reasoning)**:
模型的思考文本，作为演出流里的折叠「思考」块呈现给玩家参考；不是叙事条目，也不改变世界状态。两个来源：供应商 `reasoning_content`（`source: provider`）与模型写在正文里的 **think 意图**（`source: model`，P3）。
_实现_: `PlayEvent::Reasoning`（`ReasoningPayload.source`，serde 缺省 provider 以兼容旧数据）；引擎在主线 / 角色 AI 调用后各推一条，`think` 意图落 `source: model` 且不进叙事、不改世界状态；故事书 `narrative.display.draft`（folded / hidden，缺省 folded）控制 think 草稿是否渲染；前端用 ai-elements `Reasoning` 组件折叠展示。
_Avoid_: 内心独白（那是叙事内容，属角色演绎）

**AI 调用轨迹 (AiCallTrace)**:
一次 AI 调用（主线 / 角色）的完整日志：**发给模型的完整上下文**（system 提示词 + 会话历史原文）+ 思考链 + 解析意图 + 用量（input/output/cached tokens）+ 延迟 + 状态（ok / error）+ 尝试次数（attempts）。区别于 `reasoning`：它保留请求全文，供游玩页「日志」tab 复盘（pi 式 span：请求属性 → 事件 → 状态）。
_实现_: `PlayEvent::AiCall`（`AiCallPayload`）；rig provider 在每次补全后采集，成功 / 失败都会落事件；只进日志视图，不渲染进对话，也不改变世界状态。**自动重试（pi 式 agent 循环的 retry）**：意图解析失败时把失败原因作为纠正消息回喂模型重新输出（失败轮成对保留在会话里），最多 2 次重试；`attempts > 1` 即发生过重试。

**内容评级 (Rating)**:
故事书创作者自报的 `meta.rating`（sfw / nsfw，缺省 sfw）——**仅**驱动列表页徽标，不做内容过滤、不做年龄验证；引擎 / 提示词 / 校验都不读它。
_实现_: `storybook.meta.rating`（A 范式「世界」tab 编辑）；ST 预设导入按 NSFW 关键词推断为提案默认值；列表卡角标（P3 只做标，不做筛）。
_Avoid_: 内容分级（暗示强制过滤）、年龄验证

**演绎 (Enactment)**:
AI 基于故事书与当前世界状态生成旁白、剧情与角色言行的过程；玩家体验故事的方式。
_Avoid_: 跑团、主持

**引擎 (Engine)**:
软件内部用代码（Rust）管理规则、状态、结算与物品/技能效果的执行层；AI 通过它调用任务/物品/技能。引擎是内部组件——产品本身不是引擎，而是面向玩家的完整软件。
_Avoid_: 规则系统（引擎是领域整体，规则系统只是其一）、跑团引擎 / CRPG 引擎（产品定位禁词）

**动作协议 (Action Protocol)**:
AI 以结构化「意图」与引擎交互的协议；引擎校验→结算→回写结果。
_实现_: default / declarative 协议走**原生工具调用**（pi 式 agent：意图 = 工具，模型调用工具输出意图，`parse` 退居兜底；白名单即工具清单，模型无法输出白名单外意图）；lua 协议保持文本。供应商不支持 tools 字段时自动降级为文本协议。
_Avoid_: 工具调用协议、函数调用协议（实现名）

**意图 (Intent)**:
AI 输出的、待引擎校验的结构化操作单元（说 / 移动 / 用技能 / 用物品 / 判定 / 查询）。
_Avoid_: 工具调用、函数调用

**结算 (Resolution)**:
引擎对一次意图/技能/判定执行规则、更新状态、产出结果的过程。

**属性维度 (Attribute Dimension)**:
故事书全局定义的角色属性项（键 + 标签 + 类型：数值 / 枚举 / 文本）；人物填值，判定只用数值型。
_Avoid_: 属性、六维

**结构化骨架 (Skeleton)**:
故事书定义的章节/场景/目标/剧情触发点等结构；主线 AI 在其内即兴。
_Avoid_: 剧本、流程图

**目标 (Goal)**:
骨架中场景/章节的完成判据——声明式布尔条件（触发点已触发 / 抵达地点 / 标记已设 / 属性或关系满足等）；引擎回合末求值，达成即标记为「已达成」并提示主线 AI，不自动切场景。场景一主多副，章节聚合其下场景主目标。
_Avoid_: 任务、任务目标

**剧情触发点 (Trigger)**:
骨架场景中的条件触发式剧情节点——声明式触发条件 + 提示文本；引擎回合末求值，满足即标记「已触发」（默认一次性，可设可重复）并提示主线 AI 即兴演绎；剧情触发点本身不改世界状态。
_出处_: 对应编剧理论中场景的最小戏剧单元 **beat**（罗伯特·麦基《故事》）；系统内统一以 **trigger** 命名，凸显「条件满足才触发、触发后提示 AI」的引擎机制，而非强调剧情强度。
_实现_: 落在 `skeleton[].scenes[].triggers`（JSON 字段与代码标识符均为 trigger；条件谓词 `trigger_fired`、进度域 `trigger`）。旧格式 `beats` / `beat_fired` / `EntityRef.kind: "beat"` 由**升格**读取（schema_version 1 → 2，历史数据不改写）。
_Avoid_: 节拍（乐理术语，易误读为节奏单位）、关键时刻（暗示高潮强度，且与「目标」语义重叠）、事件（过宽，易与引擎事件混淆）、剧情点；**beat**（实现名，仅升格读取，不再使用）

**世界词条 (Lore)**:
故事书中可选的「关键词触发注入」条目——触发词 + 内容 + 优先级；引擎每回合扫玩家输入与当前场景，命中才把内容交给 AI（`constant` 条目每回合都注入；`recursive` 条目命中后其内容继续参与扫描）。用于按需补充背景而不撑爆上下文。
_出处_: 对应 SillyTavern 的 world book；本系统以 **lore** 命名，凸显它是「条件性上下文注入」而非引擎求值的剧情节点。
_实现_: 顶层 `storybook.lore`；引擎 `SessionRules::match_lore`（按 `turn_token_budget` 裁剪）；编辑器 A「词条」tab；支持从 SillyTavern 世界书 / 角色书导入。
_Avoid_: 世界书（SillyTavern 术语）、剧情触发点（Trigger 由引擎求值并提示 AI、可改进度；Lore 只注入上下文，不改世界状态）

**叙述段 (Narrative Section)** / **叙事契约 (Narrative Contract)**:
故事书里声明的「怎么讲」——一组按**槽位**（world / style / behavior / closing）注入提示词的叙述段；契约是这些段的集合。与世界词条分工：Lore 讲「世界有什么」，叙述段讲「怎么讲」。段可声明作用范围（主线 / 角色 / 两者 / 指定人物）、**注入条件**（`when`，只有世界状态满足时才注入）、以及可否被玩家调整（`playerEditable`）。
_实现_: 顶层 `storybook.narrative.sections`（A 范式「叙事」tab 可编辑）；引擎 `SessionRules::narrative` 逐回合解析、提示词按槽位渲染；校验拦截非法槽位 / 重复 id / 悬空人物引用 / 非法 `when` / 变体规则。P0 段 + P1 条件 / 变体 / 玩家偏好 + P2 协议覆盖均已实现；设计见 [叙事契约设计](./docs/narrative-contract.md) 与 [协议接口](./docs/protocol-interface.md)。
_Avoid_: 预设（外部格式名；叙事契约属于故事书，随版次发布与存档冻结）

**输出协议 (Output Protocol)** / **协议插件 (Protocol Plugin)**:
故事书可覆盖的「AI 输出格式约定」——系统层协议说明（preamble）+ 模型原始输出解析（parse），出口一律归一为引擎意图数组；模式 `default`（引擎内置）/ `declarative`（意图白名单 + 补充说明）/ `lua`。协议插件是用 Lua 实现的协议适配器（`protocol.preamble` / `protocol.parse` / 可选 `protocol.normalize`），复用既有 Lua 沙箱且只读。
_实现_: `storybook.narrative.protocol`（A 范式「协议」tab）；引擎 `ProtocolAdapter`（Default / Declarative / Lua）与 `parse_intents`；静态规则在 `validate.rs`，动态一致性 `check_protocol_conformance` 由发布门调用（草稿保存不跑，避免每次自动保存都执行 Lua）。
_Avoid_: 动作协议（AI 与引擎的固定意图接口，不可覆盖；输出协议是「怎么把模型文本变成意图」，可由故事书覆盖）、正则后处理（本系统用协议 + 意图解析，不做文本变换层）

**变体组 (Variant Group)**:
叙述段的一组互斥选项（如人称 / 节奏 / 篇幅）：段声明 `variants[{key,label,text}]` + `defaultVariant`，与 `text` 互斥；引擎每回合解析出**唯一**文本注入。
_出处_: 对标 SillyTavern 预设里「N 选 1」的提示条目。
_实现_: 解析顺序「存档玩家偏好 → `defaultVariant` → 首个变体」；发布校验拦截重复 key / 缺失或悬空的 `defaultVariant` / 变体空文本 / `text` 与 `variants` 并存。
_Avoid_: 预设（外部格式名）、分支（分支是存档命令序列的叉出，与文案选项无关）

**叙事偏好 (Narrative Override)**:
玩家对 `playerEditable: true` 叙述段的开关或变体选择——段 id → 开关(bool) | 变体 key(string)，存于**存档设置**；只影响之后的回合，绝不改写命令日志 / 历史。
_实现_: `SaveSettings.narrative` / `saves.narrative_json`（随存档冻结，不进命令日志）；引擎只认 `playerEditable` 段的偏好，非可编辑段的偏好一律忽略；游玩页「叙述偏好」对话框编辑。
_Avoid_: 预设（预设是全局采样参数档，偏好是单存档的叙述段选择）

**提示词覆盖 (Prompt Override)**:
应用级（非故事书）的提示词自定义——把引擎各处发往模型的固定文本（游玩 AI / 聊天 AI 的系统提示词与回合骨架、分节引导语、记忆压缩与纠错消息、结对 AI 的四段系统提示词）登记为具名条目，创作者在设置面板逐条覆盖；留空 = 用内置默认。
与「叙事契约」分工：叙事契约属于**故事书**（随版次发布、随存档冻结），提示词覆盖属于**应用配置**（`config.json`，不进故事书、不随存档冻结）；故事书显式声明的输出协议（declarative / lua）优先于它。
_实现_: 注册表 `octopus-api/src/prompts.rs`（key / 分组 / 默认文本 / 可用变量），默认文本集中在 `octopus-ai/src/prompt.rs`；`AppConfig.prompts` 只存覆盖；模板变量写作双花括号、单遍替换（不二次展开）；`GET /api/prompts` 返回目录（含默认值与当前覆盖），保存走 `PUT /api/config` 即改即生效。前端设置面板「提示词」分区编辑。
_Avoid_: 预设（采样参数档）、叙事契约（那是故事书的内容）

**人物 (Character)**:
故事书中定义的角色模板（背景 / 性格 / 属性值 / 掌握技能）；运行时实例化后成为角色实例。
_实现_: `characters[].skills` 引用技能 id（与物品 / 物件共用同一技能实体）；编辑器人物卡可绑定，删除技能时级联清理，校验拦截悬空引用。
_Avoid_: 角色（该词易与运行时实例混淆）

**角色实例 (Character Instance)**:
游戏运行时由人物模板实例化的角色，携带当前属性 / 位置 / 状态。
_Avoid_: 人物、NPC

**受控角色 (Controlled Character)**:
玩家当前直接控制、以其名义进行「角色输入」的角色实例；玩家可在多角色间随时切换，非受控角色由角色 AI 扮演。
_Avoid_: 主角、我的角色（暗示单一）

**技能 (Skill)**:
角色或物品可用的能力（消耗 / 冷却 / 目标 / 判定 / 效果）；声明式定义 + 可选 Lua 钩子。
_Avoid_: 法术、能力（「法术」是技能的一种分类）

**物品 (Item)**:
可携带 / 使用的物件；可引用技能成为「物品技能」。
_Avoid_: 道具

**物件 (Object)**:
故事书中可交互的场景物件（门 / 机关 / 容器等）——id + 名称 + 描述 + 动作列表，可挂技能与触发条件；AI 通过「交互」意图引用。
_Avoid_: 道具、物品（物品可携带，物件固定在场景）

**势力 (Faction)**:
故事书中的组织 / 阵营。
_Avoid_: 阵营、派系

**关系 (Relationship)**:
两个实体之间的有向边（主体 → 客体，类型 + 数值强度）；可连人物与势力两两之间。
_Avoid_: 好感度（它只是关系的一种类型）

**资源 (Resource)**:
世界定义的可消耗量（货币 / 体力 / 法力等）；被技能的消耗引用。
_Avoid_: 货币

**状态 (Status)**:
故事书全局声明的可结算持续效果（id + 名称 + 时长 + 单位 turns/scenes + 叠加策略 + 每 tick 效果）；技能 `effect.status` 与 Lua `host.apply_status` 按 id 引用；运行时以角色实例上的状态实例承载剩余回合/场景数。
_实现_: 顶层 `storybook.statuses`（A 范式「状态」tab 可编辑；schema v2→v3 由升格把旧的内联定义搬到顶层）；引擎回合 / 场景末 tick 结算每 tick 效果。
_Avoid_: 状态机（Lua 钩子旧标题的措辞；状态是可结算的持续效果，不是任意状态机）

**资产 (Asset)**:
一张图片的不可变存储单元——文件名即内容哈希（`<sha256>.<ext>`），存于应用数据目录旁的资产库，不入数据库。内容即身份 → 天然去重、永不改写，因此发布与存档冻结都**不需要拷贝**；只有导出存档包时才随包交付。
_Avoid_: 图片文件（暗示可变）、附件、BLOB（实现侧说法）

**资产引用 (AssetRef)**:
故事书 JSON 中指向资产的字段（资产名 + 像素宽高 + 可选 alt）；出现于 故事书封面 / 人物立绘 / 地点插图 / 物品图标。宽高供前端预留版面，避免加载抖动；缺省时各处回落为确定性生成的 CSS 封面或首字色块。
_Avoid_: 图片路径、URL（资产名不是路径，也不含主机）

**判定 (Check)**:
对一次行动用属性 + 难度决定成败的规则过程。判定方式由故事书声明（见「判定器」），软件不预设任何骰子体系。
_Avoid_: 检定、掷骰、d20 / DC（特定骰系的私有记法，非通用术语）

**判定器 (Checker)**:
故事书声明的判定方式——参数化配置（骰子表达式，可声明无骰由 AI 依属性叙事裁决；比较模式 ≥难度 / ≤目标值 / 对抗；属性修正映射；成功度分档阈值），或 Lua 判定脚本（自定义判定逻辑，须归一化输出最终值与差值，由引擎分档）；成功度与骰面本身无关。判定器可全局声明，也可由单个技能单独声明覆盖。
_Avoid_: 骰子系统

**玩家 (Player)**:
操作者实体；控制一个或多个角色实例。v1 单人，为多人预留。
_Avoid_: 用户、账号（系统侧词汇）

**存档 (Save)**:
一次游玩实例的全部持久化状态——内嵌创建时故事书的冻结副本、命令日志、快照与元信息的整体；完全自包含；存储于应用数据库（#27），导出为跨数据库通用的自包含存档包（`SavePackage`；zip 容器 `.octopus.zip`，内含 `save.json` + `assets/`）即分享 / 备份 / 异地迁移（旧版单 JSON 仍可导入）。
_实现_: 内嵌冻结故事书 + 命令日志已落库；「快照」部分未实现（v1 全量重放，见快照条目）。
_Avoid_: 存档文件（实现侧说法）、SQLite 二进制绑定包（必须保持跨数据库与跨平台解耦）

**开档 (New Game)**:
玩家基于某本**已发布**故事书的当前已发布版次创建新**存档**实例的入口操作；开档后进入游玩页。未发布的故事书不可开档（#23/#24）。区别于「继续游玩」（对既有存档恢复会话）。
_Avoid_: 开始游戏、新建游戏（过泛，未指明基于已发布故事书版次创建存档）

**继续游玩 (Resume)**:
列表页对最近游玩的存档直接恢复会话的入口动作（取 last_played_at 最近者直达）；对需升级存档仍直达，升级提示归游玩页横幅（#25）。

**命令 (Command)**:
经引擎校验通过并结算完毕的状态变更单元，命令日志的条目；区别于 AI 输出的原始「意图」——被驳回的意图不是命令。
_实现_: 命令日志已落库；当前实现里命令与叙事条目共用 `commands` 表，以 `kind` 区分。
_Avoid_: 事件（过宽）

**叙事条目 (Narrative Entry)**:
命令日志中记录 AI 叙事产物（旁白 / 对话 / 神态）的权威条目——不改变世界状态，但可重放、可回读（#17 的 history 端点）。
_实现_: 已落地——演出事件按序存于 `commands` 表（payload 为完整 `EventEnvelope`），`/api/saves/{id}/history` 回读。
_Avoid_: 叙事产物（过泛，且与非权威的摘要 / 向量索引混淆）

**命令日志 (Command Log)**:
存档的权威事实来源；append-only，世界状态由它重放物化；历史条目永不改写（旧格式读取时升格）；记录命令与叙事条目。永不自动截断——仅玩家显式触发的「新原点」维护操作可截断。
_实现_: 已落地——每次 `emit` 先写库、后广播（write-ahead），并把新增的 RNG 消耗写成 `rng_consume` 事件；重建会话时从日志**纯投影重放**、据消耗次数复位 `DeterministicRng`（骰序跨重启连续），绝不重跑 AI。截断（新原点）未实现。
_Avoid_: 事件流（实现名）

**升格 (Upcast)**:
读取旧格式的日志条目 / 快照 / 内嵌故事书时，按版本链式转换为当前结构的过程；历史数据本身永不改写。
_实现_: 已落地**首条版本链**（`crates/octopus-engine/src/upcast.rs`）：故事书 v1 → v2（骨架 `beats` → `triggers`、条件 `beat_fired`/`beat_id` → `trigger_fired`/`trigger_id`、`EntityRef.kind: "beat"` → `"trigger"`）与 v2 → v3（技能内联 `effect.status` → 顶层 `statuses` + 技能侧 id 引用）；接入读取路径——故事书草稿 / 已发布版次、存档内嵌冻结副本、存档包导入、命令日志重放。`DeltaDomain` 的旧 `beat` 由 serde alias 兼容。快照升格仍随快照未实现。

**遗留区 (Legacy Zone)**:
版次升级时，被「遗留冻结」处理的人物旧定义在存档内的存放区域；实例继续可用，旧定义只读保留。
_实现_: **未实现**。
_Avoid_: 冻结区、存档区（过宽）

**快照 (Snapshot)**:
某一命令序号上世界状态的全量物化；仅作启动加速的缓存，任何快照都可从命令日志重建。
_实现_: 已落地——`snapshots` 表（同事务保留最新 5 份）；启动走「最新可用快照 + 重放其后命令」，快照缺失 / 格式或版次过期 / JSON 损坏 / `seq` 越界一律回退全量重放；RNG 位置随 `WorldState.rng_position` 携带，骰序跨重启连续。手动存档与每 10 回合自动写入。快照是派生缓存，导出存档不依赖它。

**新原点 (New Origin)**:
玩家显式触发的存档维护操作——以某快照为存档新起点，此前的命令日志归档进库内归档表（只读）；回放 / 回滚以新原点为界。v1 玩家侧可见（#21/#24）。
_实现_: **未实现**——端点返回 `501 not_implemented`，前端按钮标注「暂不可用」（压缩会破坏重放恢复语义）。

**维护历史 (Maintenance History)**:
存档 meta 中记录维护操作的只读日志（何时、从版次 X→Y / 新原点压缩、报告摘要）；升级本身不是命令、不进命令日志。
_实现_: 已落地——`maintenance` 表 + `/api/saves/{id}/maintenance`。
_Avoid_: 操作日志（易与命令日志混淆）

**分支 (Branch)**:
回滚产生的新命令序列——从某个历史命令序号叉出，旧序列原样保留。
_实现_: **未实现**——回滚 / 分叉尚未落地；「多开会话」不依赖它（见「结对会话线程」，线程只隔离对话，不叉出命令序列）。
_Avoid_: 时间线

**演出流 (Presentation Stream)**:
引擎结算结果推送给前端的派生事件流（SSE 长连接，引擎→UI 单向推送）；驱动三种演出模板呈现；**非权威**——权威是命令日志。
_实现_: 已落地——SSE `/api/saves/{id}/stream`；前端内置指数退避重连与按 seq 水位补拉。
_Avoid_: 事件流（与命令日志混淆；「事件流」在命令日志条目下已是禁词）

**确认门 (Confirmation Gate)**:
有代价的机制动作（技能 / 判定 / 资源消耗）结算前的玩家确认环节——引擎暂停并等待确认 / 取消（超时默认取消）；确认期间回合保持进行；全局「免确认」为引擎侧开关，开启后不再询问、直接结算。

**演出模板 (Presentation Template)**:
叙事呈现层的形态（聊天流 / 剧本式 / 沉浸式）；同一演出流、同一回合模型，仅呈现不同；切换为纯前端本地行为，不进事件 schema。

**编辑范式 (Editing Paradigm)**:
故事书编辑器的三种工作形态——A 表单工作台（有计划详细编辑）、C AI 结对（无头绪启发式编辑）、B 文档（叙事自由书写）；三者操作同一草稿、以视图态切换，纯前端本地行为，不进编辑协议。

**结对 (Pairing)**:
C 编辑范式的 AI 协作——玩家与 AI 边聊边成型故事内容；对话按「结对会话线程」持久化。**AI 的任何改动都只是待审查提案，从不直接改草稿**；经创作者批准后才经编辑操作落入草稿。
_实现_: 已落地——对话按线程落库；工具改动先进暂存草稿，批准后才合并（草稿零污染）。待审查建议落 `pair_threads.pending_suggestions_json`，刷新 / 切会话 / 重进都能恢复。
_Avoid_: 聊天（过宽，结对产出的是可采纳的故事内容而非闲谈）

**结对会话线程 (Pair Thread)**:
C 范式下**一本故事书内的多条** AI 协作会话——每条线程独立持久化消息与上下文（**不做轮次裁剪，按全量历史发送**），可新建 / 切换 / 重命名 / 删除；用于按主题隔离（如一条讨论任务、一条讨论物品）。线程只隔离对话与上下文，不改变存档的世界状态，也**不是「分支」**。
_实现_: 已落地——`pair_threads` + `pair_messages.thread_id`，C 范式左栏可管理；状态行报真实 token 用量（对话 / 故事书 / 工具 + 上轮出字 / 速度 / 缓存命中）。
_Avoid_: 聊天记录（过宽）、分支（分支是存档命令序列的叉出，与本概念无关）

**引用 (Entity Ref)**:
创作者在对话里显式指向某个故事书实体的结构化标记（kind + id + 可选 parent_id + 名称）——用于精准指定「说的是 / 要改的是这个」，避免模型从名称猜测。结对（改草稿）与游玩（玩家行动）共用同一形状；被引用的实体会以**完整定义**随本轮请求交给 AI，并作为本轮的主要目标。
- 结对：随消息持久化（`pair_messages.refs_json`）。
- 游玩：随回合持久化（`RoundInput.refs`，落进命令日志，重放 / 刷新一致）；完整定义仅本轮透传、不落库。
_实现_: 已落地——两个聊天输入框均有「引用」按钮 / `@` 唤起共享的实体选择器（`@/components/EntityPicker.vue`）；后端 `focus` 注入完整定义（结对 `build_system_prompt`、游玩 `turn_prompt`）。
_Avoid_: @提及（@ 只是唤起选择器的一种快捷方式，引用本身是结构化数据）

### 规则建模

> D&D 等具体规则体系的术语与本项目通用术语的对照见 [术语消歧](./docs/terminology-disambiguation.md)。

**开放种类 (Kind)**:
故事书自己声明的「内容种类」——key + 显示名 + 字段 schema；`applies_to` 声明它能挂接到哪些实体（如人物）。代码不枚举种类，新增一个故事概念 = 一条数据。
_Avoid_: 实体类型、schema（种类是内容词汇，不是代码类型）

**开放内容 (Definition)**:
按某个开放种类填出的一条具体内容（kind + id + 名称 + 字段值）；种类是模板，内容是实例。
_Avoid_: 实体（易与运行时角色实例混淆）；**用开放内容重复机制核心**（属性维度 / 派生值 / 资源 / 装备 / 状态）——那些是封闭语义、各有专用声明；开放种类只装规则书内容（种族 / 职业 / 背景 / 特性 / 语言…）

**挂接 (Attachment)**:
把一个开放内容指派给某个实体的持久关系（如把「人类」挂到某个人物）——`attachments: 种类 key → 内容 id 列表`；没有它，开放内容只能停在扩展区。
_Avoid_: 关联、引用（引用是对话里的临时指向，挂接是持久指派）

**派生值 (Derived Value)**:
由公式从属性维度与修正来源算出的二级数值（调整值 / 豁免 / AC / 法术DC / 被动察觉）；声明式、按顺序可互相引用。
_Avoid_: 计算属性、二级属性

**修正来源 (Modifier Source)**:
对某个属性维度或派生值施加增量的来源（挂接的定义 / 已装备的物品）；叠加规则为 add 累加 → max 取高 → set 覆盖。
_Avoid_: 加成、buff（buff 是可结算的持续状态，不是数值来源）

**判定种类 (Check Kind)**:
一次判定的四种形态——属性检定（本人掷骰 vs 难度）/ 攻击检定（本人掷骰 vs 目标防御）/ 豁免（**目标**掷骰 vs 施加方 DC，失败才结算效果）/ 被动值（不掷骰，基数 + 修正）。
_Avoid_: 检定类型；注意**豁免不是一种属性检定**（掷骰方与结算方向都不同）

**资源层阶 (Resource Tier)**:
可消耗资源的分层标注（如法术位按环）；只影响分组与展示，不改变结算。
_Avoid_: 等级（等级是角色成长概念）

**恢复时机 (Recovery Trigger)**:
资源自然恢复的触发点——每回合 / 每场景 / 短休 / 长休（`per_rest` 视为长休别名）。长休补满到上限，短休只补短休项且封顶。
_Avoid_: 回蓝、回复

**休息 (Rest)**:
玩家显式触发的资源恢复操作（短休 / 长休）；对声明了恢复时机的资源产出状态增量，并写入命令日志。
_Avoid_: 睡眠（那是某个具体技能的效果）

**每日准备 (Prepared)**:
角色从已掌握技能中每日挑选的子集（如 D&D 的 prepared spells）；长休后重挑，属于角色而非故事书。
_Avoid_: 已学习（「掌握」是永久，「准备」是每日）

**装备位 (Equip Slot)**:
可装备物品声明的部位（armor / shield / weapon…）；同一角色同一位只能装备一件，装备后其修正并入派生计算。
_Avoid_: 背包（背包是持有，装备位是生效）

**卡面分区 (Sheet Section)**:
故事书声明的角色卡展示结构——分区标题 + 每区放哪些挂接种类 / 派生值 / 资源 / 叙事字段；未声明时面板走内置布局。
_Avoid_: 面板（面板是实现，分区是展示结构）

**熟练项 (Proficiency)**:
具体规则体系（如 D&D）中的「技能 / 豁免 / 护甲 / 武器」熟练。**与本项目的「技能」不是一回事**：本项目「技能」是可被使用的能力，规则里的技能熟练应作为开放内容（如 `dnd-skill` 种类）挂接。
_Avoid_: 技能（本项目的「技能」专指可用能力，见「技能」条）

**属性 (Ability)**:
具体规则体系对可判定数值槽的称呼（D&D 六维）。本项目对应的通用概念是**属性维度**；「属性」只在讨论具体规则体系时使用。
_Avoid_: 属性维度（本项目通用术语，见「属性维度」条）

